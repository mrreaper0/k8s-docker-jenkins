pipeline {
  agent any
  stages {
    stage('Run Ansible File') {
      steps {
        sh 'ansible-galaxy collection install kubernetes.core'
        sh 'ansible-playbook --user=server --extra-vars "ansible_sudo_pass=13998" -i Ansible-files/inventory Ansible-files/main.yaml'
      }
    }
  }
}
