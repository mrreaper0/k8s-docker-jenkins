pipeline {
  agent any
  stages {
    stage('Run Ansible File') {
      steps {
        sh 'ansible-galaxy collection install kubernetes.core'
        sh 'ansible-playbook -i Ansible-files/inventory Ansible-files/main.yaml'
      }
    }
  }
}
