pipeline {
  agent any
  stages {
    stage('Run Ansible File') {
      steps {
        sh 'ansible-galaxy collection install kubernetes.core'
        sh 'ansible-playbook --ask-sudo-pass -i Ansible-files/inventory Ansible-files/main.yaml'
      }
    }
  }
}
