pipeline {
  agent any
  stages {
    stage('Run Ansible File') {
      steps {
        sh 'ansible-galaxy collection install docker-compose'
        sh 'ansible-playbook Ansible-files/main.yaml'
      }
    }
  }
}
